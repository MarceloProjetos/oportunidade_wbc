hdbcli==2.29.23
supabase==2.31.0
pandas==2.2.3
python-dotenv==1.2.2
pyodbc==5.3.0
apscheduler==3.10.4
# fixado para garantir websockets.asyncio (corrige ModuleNotFoundError)
websockets==15.0.1
# API HTTP de disparo da sync de OS por NPED (api.py) — waitress p/ produção (Windows)
flask>=3.0,<4
waitress>=3.0,<4
# lock de arquivo (cross-process) p/ serializar a carga de oportunidades (scheduler + API)
filelock>=3,<4
# cliente HTTP do SAP Service Layer (ordens_producao_sl.py — escrita de status de OP).
# Vinha só por transitividade; agora é dependência DIRETA. Mesma lib usada pelos clientes
# de Service Layer do web_orcaview_V117, de propósito: os módulos ficam diffáveis.
requests>=2.32,<3
# opcional: métricas de CPU/memória no endpoint /status (sem ele, o /status funciona,
# mas CPU e memória ficam indisponíveis). Instale no servidor: pip install psutil
psutil>=5,<8

# --- Integração WBC → SAP (pacote wbcpython/: worker + painel) ---
# Pins abertos por maior/menor: a .11 instala pelo pip do Python 3.12 do sistema.
# httpx, hdbcli, apscheduler e python-dotenv já vêm acima (mesmas libs, de propósito).
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
sqlalchemy>=2.0,<3
# SQL Server do WBC (WBCCAD, SOMENTE LEITURA) via SQLAlchemy — driver diferente do pyodbc
# usado pelo pipeline de oportunidades; o repositório do WBC nasceu sobre pymssql.
pymssql>=2.3,<3
# painel de acompanhamento (HTML servido por FastAPI + Jinja2, HTMX versionado no pacote)
fastapi>=0.111,<1
uvicorn>=0.30,<1
jinja2>=3.1,<4
python-multipart>=0.0.9,<1
